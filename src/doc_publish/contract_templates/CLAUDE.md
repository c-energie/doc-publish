# CLAUDE.md

`AGENTS.md` beside this file is the contract for every agent working in this document,
and it is tool-neutral by design. Claude Code does not read that filename on its own, so
this file exists only to import it.

Keep the guidance in `AGENTS.md`. A second copy here would be out of date within a month,
and the two would disagree with no way to tell which was right.

@AGENTS.md
